{
  "name": "online-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Customer order portal proof of concept for Tank Nutrition.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js",
    "check:all": "node --check server.js && node --check public/app.js && node --check netlify/functions/app.mjs && node --check scripts/reset-local-data.mjs && node --check scripts/smoke-local.mjs",
    "reset:local": "node scripts/reset-local-data.mjs",
    "smoke:local": "node scripts/smoke-local.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0"
  }
}
